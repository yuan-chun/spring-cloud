spring:
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config-server
      #对应{application}部分
      name: config
      #对应{profile}部分
      profile: prod
      label: master
management:
  security:
    enabled: false